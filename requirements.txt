# geo-herd-rider dependencies.
#   anthropic         curator: Claude + web search (causal-chain reasoning)
#   yfinance, pandas  scoreboard + optimizer: prices, returns
#   numpy, scipy      optimizer: mean-variance solve
#   pyyaml            investor_profile.md front-matter parsing
#   requests          polymarket: Gamma/CLOB REST (probability signal)
anthropic>=0.40
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26
scipy>=1.13
pyyaml>=6.0
requests>=2.31
matplotlib>=3.8     # scripts/plot_shipping.py (README figures)
